<h1>Onion Scrapes</h1>
<div>
    {{#each articles}}
		<div class = "row" style="padding: 30px">
			<div class = "panel panel-default">
				<div class = "panel-heading"><a href="{{link}}" target="_blank"><h3>{{title}}</a></h3> </div>
				<div class = "panel-body"><p>{{excerpt}}</p></div>
				<!-- <p>{{excerpt}}</p> -->
			<button type="button" class="btn btn-primary saved">Save this article</button>	
			</div>
				
					

   		</div>
    {{/each}}

</div>